# NOTE(koekeishiya): A list of all built-in modifier and literal keywords can
# be found at https://github.com/koekeishiya/skhd/issues/1

## APPLICATION FOCUS SHORTCUTS

# App switching with Ctrl+key (near home row)
# Focus Google Chrome
# ctrl - h : yabai -m window --focus $(yabai -m query --windows | jq '.[] | select(.app=="Google Chrome") | .id') || open -a "Google Chrome"
# ctrl - h : yabai -m space --focus 1
ctrl - h : yabai -m space --focus 1

cmd + alt - x : yabai -m space --focus recent
cmd + alt - 1 : yabai -m space --focus 1


# send window to desktop and follow focus
ctrl + alt - u : yabai -m window --space next; yabai -m space --focus next
ctrl + alt - i : yabai -m window --space  2; yabai -m space --focus 2

# Focus WezTerm
ctrl - o : yabai -m window --focus $(yabai -m query --windows | jq '.[] | select(.app=="WezTerm") | .id') || open -a "WezTerm"

# Focus Spotify
ctrl - i : yabai -m window --focus $(yabai -m query --windows | jq '.[] | select(.app=="Spotify") | .id') || open -a "Spotify"

# Toggle between last two apps
ctrl - u : yabai -m window --focus recent

# Launch & maximize apps
ctrl + shift - p : ~/.yabai_maximize.sh "Google Chrome"
ctrl + shift - o : ~/.yabai_maximize.sh "WezTerm"
ctrl + shift - i : ~/.yabai_maximize.sh "Spotify"

## WINDOW MANAGEMENT

# Focus window
alt - h : yabai -m window --focus west
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - l : yabai -m window --focus east

# Swap window
shift + alt - h : yabai -m window --swap west
shift + alt - j : yabai -m window --swap south
shift + alt - k : yabai -m window --swap north
shift + alt - l : yabai -m window --swap east

# Move window
shift + cmd - h : yabai -m window --warp west
shift + cmd - j : yabai -m window --warp south
shift + cmd - k : yabai -m window --warp north
shift + cmd - l : yabai -m window --warp east

# Balance size of windows
shift + alt - 0 : yabai -m space --balance

# Resize windows
ctrl + alt - h : yabai -m window --resize left:-50:0; yabai -m window --resize right:-50:0
ctrl + alt - j : yabai -m window --resize bottom:0:50; yabai -m window --resize top:0:50
ctrl + alt - k : yabai -m window --resize top:0:-50; yabai -m window --resize bottom:0:-50
ctrl + alt - l : yabai -m window --resize right:50:0; yabai -m window --resize left:50:0

# Toggle window fullscreen zoom
alt - f : yabai -m window --toggle zoom-fullscreen

# Toggle window native fullscreen
shift + alt - f : yabai -m window --toggle native-fullscreen

# Toggle floating window
shift + alt - space : yabai -m window --toggle float; yabai -m window --grid 4:4:1:1:2:2

# Move window to space
shift + alt - 1 : yabai -m window --space 1
shift + alt - 2 : yabai -m window --space 2
shift + alt - 3 : yabai -m window --space 3
shift + alt - 4 : yabai -m window --space 4
shift + alt - 5 : yabai -m window --space 5

# Focus space
ctrl - 1 : yabai -m space --focus 1
ctrl - 2 : yabai -m space --focus 2
ctrl - 3 : yabai -m space --focus 3
ctrl - 4 : yabai -m space --focus 4
ctrl - 5 : yabai -m space --focus 5

## SIMULATING DOUBLE-TAP FUNCTIONALITY
# Note: skhd doesn't natively support double-tap, so we use a timeout approach
# These sequences simulate double-tap with a short timeout (300ms)

# Double-tap "p" (for Chrome)
p -> p : yabai -m window --focus $(yabai -m query --windows | jq '.[] | select(.app=="Google Chrome") | .id') || open -a "Google Chrome"

# Double-tap "o" (for WezTerm)
o -> o : yabai -m window --focus $(yabai -m query --windows | jq '.[] | select(.app=="WezTerm") | .id') || open -a "WezTerm"

# Double-tap "i" (for Spotify)
i -> i : yabai -m window --focus $(yabai -m query --windows | jq '.[] | select(.app=="Spotify") | .id') || open -a "Spotify"

# Restart yabai
shift + alt + cmd - r : yabai --restart-service && skhd --restart-service

# Move window to space
